### Condition Expression
เป็นเงื่อนไขแบบย่อที่เขียนจบได้ภายในบรรทัดเดียว (อีกชื่อหนึ่งคือ ternary opertator) โดยมีรูปแบบคำสั่งดังนี้

    A if conditon else B

โดยหากเงื่อนไขเป็นจริง จะส่งคืนค่า A แต่เป็นเท็จจะส่งคืน B

*มักใช้ในการกำหนดข้อมูลให้กับตัวแปร ตามเงื่อนไขที่กำนหนด*