### Type Casting
เป็นการแปรงชนิดข้อมูล จากชนิดข้อมูลชนิดหนึ่งไปเป็นชนิดอื่น ๆ ที่เราต้องการ โดยมีฟังก์ชันให้ใช้งานดังนี้
1. str() แปรงข้อมูลเป็นข้อความ
2. int() แปรงข้อมูลเป็นเลขจำนวนเต็ม
3. float() แปรงข้อมูลเป็นเลขจำนวนเต็มพร้อมทศนิยม
4. bool() แปรงข้อมูลเป็นค่าจริง/เท็จ